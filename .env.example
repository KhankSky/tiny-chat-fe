# Public URL of the backend API. This value is embedded into the frontend at build time.
# Example: https://api.example.com
NEXT_PUBLIC_API_URL=

# OAuth client ID used by the Google sign-in button.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
